<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="project_project_line_view_tree" model="ir.ui.view">
        <field name="name">project.project.line.tree (Helpdesk Maintenance)</field>
        <field name="model">project.project.line</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name" string="Name"/>
                <field name="project_id" widget='many2many_tags' />
                <field name="project_ids_count" string='Nº Equipment'/>
            </tree>
        </field>
    </record>

    <record id="project_project_line_view_form" model="ir.ui.view">
        <field name="name">project.project.line.form (Helpdesk Maintenance)</field>
        <field name="model">project.project.line</field>
        <field name="arch" type="xml">
            <form>
              <sheet>
                    <group>
                
                        <field name="name" string="Name"/>
                        <field name="project_id" />
                    </group>
                </sheet>
            </form>
              
        </field>
    </record>

</odoo>
